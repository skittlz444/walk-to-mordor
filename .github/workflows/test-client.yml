name: Client Tests

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  checks: write
  pull-requests: write

env:
  NODE_VERSION: '22'

jobs:
  client:
    name: Client Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Client Unit Tests & Coverage
        run: npm run test:client:coverage

      - name: Upload Coverage Reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-client
          path: coverage/client/
          retention-days: 30

      - name: Publish Test Results
        if: always()
        uses: EnricoMi/publish-unit-test-result-action@v2
        with:
          files: test-results/client/junit.xml
          check_name: Client Tests
          comment_mode: always

      - name: Generate Coverage Comment
        id: gen-coverage
        if: always()
        run: node .github/scripts/generate-single-coverage-comment.js --suite client

      - name: Post Coverage Comment
        if: always() && steps.gen-coverage.outcome == 'success'
        uses: marocchino/sticky-pull-request-comment@v3
        with:
          header: coverage-client
          path: coverage-comment.md
